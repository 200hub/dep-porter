[nexus]
base_url = "http://localhost:8081/"
username = "admin"
password = "12345678"

[repositories]
maven = "maven-releases"
maven_snapshots = "maven-snapshots"   # 可选，不填则 SNAPSHOT 也发到 maven
npm = "npm"
pypi = "pypi"
cargo = "cargo"                # 可选，不填则走 raw 兜底
raw = "raw"
